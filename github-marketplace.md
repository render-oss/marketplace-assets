![Banner](https://marketplace-assets.onrender.com/images/banner.png)

Render is the cloud for builders, with intuitive infrastructure to scale any app or agent from your first user to your billionth. Push code, go live. No cold starts, no timeouts, no surprise bills.

Install the Render app, then connect any repository to get:

- **Automatic deploys on push**, with zero-downtime rollouts and instant rollback
- **Preview environments** for every pull request, fully managed and torn down on merge
- **Deployment status on every commit and PR**, linked straight to logs in your Render dashboard

![PR PREVIEW IMAGE](https://marketplace-assets.onrender.com/images/deploy.png)

### Whatever your stack, it runs on Render

- **Web Services** for APIs, SSR apps, and long-lived backends
- **Static Sites** served from a global CDN, free TLS included
- **Background Workers** and **Cron Jobs** for async work and scheduled tasks
- **Workflows** for durable, long-running AI agents, background jobs, and data pipelines 
- **Private Services** on an isolated network, no public ports required
- **Managed Postgres** with HA, read replicas, and point-in-time recovery
- **Key Value** (Redis-compatible) for caching and queues

![ARCHITECTURE DIAGRAM](https://marketplace-assets.onrender.com/images/diagram.png)

### Intuitive infrastructure, designed for builders

- **Load-based autoscaling** that handles 100x traffic bursts and beyond
- **Persistent Disks** for data that sticks around across deploys
- **Private Networking** for secure service-to-service communication
- **Infrastructure as Code** via `render.yaml` or Terraform
- **Native observability**: metrics, logs, alerts, and OpenTelemetry exports
- **REST API, CLI, MCP server, and webhooks** to automate anything
- **MCP server, AI rules, and agent skills** so Claude Code, Cursor, Codex, and other coding agents ship on Render natively

![DASHBOARD IMAGE](https://marketplace-assets.onrender.com/images/dash.png)

### Why builders pick Render

No serverless timeouts for agents that think for minutes. Render gives AI workloads what they actually need: long-running containers, WebSockets, persistent state, and durable workflows via the Render SDK. No per-seat tax that punishes growing teams. No egress meter spinning every time a user loads a page. Predictable pricing and bundled bandwidth, so your AI-native stack scales without blowing up your margins.

SOC 2 Type 2, HIPAA, ISO 27001, and GDPR ready.  

![RENDER CUSTOMER](https://marketplace-assets.onrender.com/images/cust-logos.png)

**Zero ops, zero surprises.**  Start free at [render.com](https://render.com).
